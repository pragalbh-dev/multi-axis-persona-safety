# HuggingFace token — required for gated models (Gemma 2 27B) and all model downloads.
# Generate at https://huggingface.co/settings/tokens; read-only access is sufficient.
HF_TOKEN=

# Optional: Weights & Biases. Not used in core pipeline.
# WANDB_API_KEY=
